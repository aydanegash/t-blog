<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>  the blog spot</title>
  <link rel="stylesheet" href="/css/style.css">
   <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css">
  {{!-- <link rel="stylesheet" href="/css/jass.css"> --}}
</head>

<body>
  <div class="nav-wrapper">
      <nav>
          <a href="/" class="brand-logo">the blog spot</a>
          <ul id="nav-mobile" class="right hide-on-med-and-down">
            {{#if logged_in}}
            <li><a href="/profile">+ new post</a></li>
            <li><a href="/" id="logout">logout</a></li>
             {{!-- <li><a <button class="no-button" id="logout">Logout</button>><li> --}}
            {{else}}
          <ul id="nav-mobile" class="right hide-on-med-and-down">
            <li><a href="/login">login </a></li>
            {{/if}}
          </ul>
        </nav>
    </div>

   {{!-- <header class="display-flex justify-space-between align-center p-2"></header>  
  </header> --}}

    <main class="container container-fluid mt-5">
      <!-- Render the sub layout -->
      {{{ body }}}
    </main>

    <footer>
    <div class="copyright">
      <p>&copy2021 &nbsp; the blog spot</p>
    </div>
    </footer>
  </div>

  <!-- Render script for logged in users only -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}

   <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js"></script>
</body>

</html>
